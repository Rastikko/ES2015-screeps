"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
var setObjective = function setObjective() {
  // Revamp of set action
};

var towardsObjective = function towardsObjective(objective) {
  // Check if move
  // Check if objective defined
  // return true or false
};

var moveTowards = function moveTowards(objective) {
  // Grab object from memory
  // Check if we get as close possible
  // Stop moving towards and return
  // Move towards the objective
};

exports.default = setObjective;
module.exports = exports['default'];